/**
 * Chat page — conversational AI interface.
 *
 * Renders the message list, chat input, and HITL approval cards.
 * Connects to POST /api/v1/chat/message for sending messages.
 * Renders inline Plotly charts from chart_data in the response.
 *
 * Note:
 *   Core chat logic, streaming handling, and state management are
 *   proprietary and not included in this public version.
 */

export default function ChatPage() {
  throw new Error("Proprietary implementation — not included in public version");
}
