/**
 * KPI Dashboard page.
 *
 * Displays metric cards and trend charts for all connected data sources.
 * Fetches data from GET /api/v1/kpi/summary with a date range selector.
 *
 * Note:
 *   Dashboard implementation is proprietary and not included in this public version.
 */

export default function DashboardPage() {
  throw new Error("Proprietary implementation — not included in public version");
}
